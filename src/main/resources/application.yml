spring:
  application:
    name: POS-GunStore
  datasource:
    url: jdbc:postgresql://localhost:5432/gunpos
    username: postgres
    password: postgres
  jpa:
    hibernate:
      ddl-auto: update    # use migrate via Flyway; use update only for quick dev
      show-sql: true
jwt:
  secret: "mysuperlongsecretkeyforjwtthatismorethan32chars"
  expiration-ms: 3600000
storage:
  type: s3
  s3:
    endpoint: "https://sfo3.digitaloceanspaces.com"
    region: "sfo3"
    bucket: "posgunstore"
    accessKey: "DO00L242X3448FG6CEJC"
    secretKey: "IgkO6WPBho2+smVwMEV8noLQ+w+fG+PZFkMbu32Mnxs"
file:
    storage:
      local-root: "/var/data/app-files"

backup:
  source-dir: "/var/data/app-data"

